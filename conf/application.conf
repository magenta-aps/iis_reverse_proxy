# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="verysecret"

# The application languages
# ~~~~~
application.langs="da"

# Logger
# ~~~~~
# Logging is setup in the applictaion-logger.xml

# SSL settings
# ~~~~~
keystorefile = "/etc/java/security/jssecacerts"
keystorepassword = "changeit"

# LDAP configuration
# ~~~~
ldap.hostname = "ad.example.com"
ldap.port = 636
ldap.basedn = "dc=example,dc=com" 
ldap.usergrouprdn = "OU=people"
ldap.userattribute = "cn"
ldap.authorizedgrouprdn = "cn=authorized"
ldap.authorizedattribute = "member"

# CPR Broker configuration
# accesslevel can be 0/1/2 which represents localOnly/localThenExternal/externalOnly
# ~~~~
cprbroker.endpoint = "https://cprbroker.example.com/Services/Part.asmx"
cprbroker.applicationtoken = "33w2e3e1-4637-8079-bdf4-b3fa99d7dcc4"
cprbroker.usertoken = "CPReader"
cprbroker.accesslevel = 0